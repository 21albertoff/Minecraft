#Configurar como se detecta la vulnerabilidad
channels: ["MC|BEdit", "MC|BSign"]
max-size: 30000

#Comanda a despachar cuando la explotación es detectada.
#%player% será reemplazado con el nombre del jugador.
commands:
  - 'ban %player% Jigsaw Crash'
